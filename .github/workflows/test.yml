name: Tests

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv and Python
        uses: astral-sh/setup-uv@v5
        with:
          # This replaces "uv python install" and "setup-python"
          python-version: ${{ matrix.python-version }}
          enable-cache: true

      - name: Run tests
        # This one command replaces venv creation, pip install, AND activation
        run: uv run --extra test pytest -v --cov=alphago
